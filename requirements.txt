Django==5.1.5
gunicorn==23.0.0          # WSGI server for Render/Heroku
psycopg2-binary==2.9.10   # if you’ll use Postgres later
djangorestframework==3.15.2  # only if you really use DRF
python-dotenv==1.0.1         # only if you load .env files
